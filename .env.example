# Contoh .env untuk pengembangan lokal
# Salin file ini menjadi .env dan isi nilai yang sesuai

DB_HOST=localhost
DB_NAME=dgitech_db
DB_USER=root
DB_PASS=

# OpenAI API key (jangan commit ke repo)
OPENAI_API_KEY=sk-your-openai-key-here

# SMTP / Mail settings (untuk testing lokal gunakan MailHog)
# SMTP_HOST kosong berarti gunakan PHP mail() fallback
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=
MAIL_FROM="DGITECH" <no-reply@localhost>
